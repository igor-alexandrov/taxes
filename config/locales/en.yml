# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    errors:
      models:
        estimated_payment:
          amount_cannot_be_reduced: Amount cannot be reduced
          amount_cannot_be_more_then_annual_amount: Amount cannot be more then annual amount
          amount_cannot_be_more_then_dept_amount: Amount cannot be more then dept amount
          amount_is_too_big: Amount is too big
